import styled from 'styled-components';

const Th = styled.th`
  padding-left: 24px;
  color: #757575;
  font-size: 14px;
  line-height: 16px;
  font-weight: 500;
  text-align: left;
  cursor: pointer;
`;

export default Th;
